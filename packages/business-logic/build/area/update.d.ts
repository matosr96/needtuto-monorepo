import { Area } from "@needtuto/types";
export declare const updateArea: (uuid: string, name: string, description: string, category: string, hour_value: number, tutoring_type: string) => Promise<Area | Error>;
